{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run deploy:check:frontend && npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
